orbs:
  node: circleci/node@4.2.1

version: 2.1

workflows:
  app-tests:
    jobs:
      - node/test
      
job:
  node/test:
    docker:
      - image: cimg/node:14.13.0
    steps:
      - checkout
      - run: node --version
