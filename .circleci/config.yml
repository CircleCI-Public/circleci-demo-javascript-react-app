version: 2.1

parameters:
  branch:
    type: string
    default: "main"

jobs:
  build:
    docker:
      - image: cimg/base:2021.07
    steps:
      - checkout
      - run: echo "<< pipeline.git.branch >>"

workflows:
  workflow1:
    jobs:
      - build:
          context:
            - << pipeline.git.branch >>