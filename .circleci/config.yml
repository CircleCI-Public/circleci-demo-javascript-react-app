version: 2.1

workflows:
  version: 2
  test-deploy:
    jobs:
      # - test
      - test_dev:
          filters:
            branches:
              only:
                -rayondev

jobs:
  test:
    docker:
      - image: cimg/node:14.13.0
    steps:
      - checkout
      - run: node --version
      - run:
          name: Install libs
          command: npm install
      - run:
          name: Build extension
          command: npm run build      
  test_dev:
    docker:
      - image: cimg/node:14.13.0
    steps:
      - checkout
      - run:
          name: Install libs
          command: npm install
      - run: 
          name: Build project
          command: npm run build



