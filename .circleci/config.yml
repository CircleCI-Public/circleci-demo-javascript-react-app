orbs:
  node: circleci/node@4.2.1

version: 2.1

jobs:
   build:
      docker:
        - image: beknazar007/k8sphp
      steps:
        - setup_remote_docker:
            docker_layer_caching: true
        - run: yum -y update
        - run: yum -y install  httpd
        - run: yum -y install php
        - run: echo 'Hello from ZERO' > /var/www/html/index.html
        - cmd: ['/usr/sbin/httpd','DFOREGROUND'] 
        - expose: 80
        
   build1:
      docker:
        - image: docker
      steps:
        - setup_remote_docker:
            docker_layer_caching: true
        - run: docker image ls
        - run: docker volume ls
        - run: docker ps
        - run: docker image 
        - run: docker ps   
workflows:
  app-tests:
    jobs:
      - build
      - build1 