version: 2.1

jobs:
  docker-purge:
    docker:  
      - image: cimg/base:2022.06-22.04 
    parallelism: 50
    steps:
      - setup_remote_docker
      - run: docker volume prune -f

workflows:
  your-workflow:
    jobs:
      - docker-purge