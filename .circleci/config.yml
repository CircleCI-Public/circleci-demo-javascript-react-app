version: 2.1

jobs:
  test-ci-install:
    macos:
      xcode: "13.3"
    resource_class: macos.x86.metal.gen1
    steps:
      - checkout
      - run:
          command: |
            echo "hi"
workflows:
  test-ci-workflow:
    jobs: 
      - test-ci-install